[package]
name = "gasleak"
version = "0.1.0"
edition = "2024"

[dependencies]
aws-config = "1"
aws-sdk-cloudtrail = "1"
aws-sdk-ec2 = "1"
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
